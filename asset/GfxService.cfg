[config]
{
    WINDOW_WIDTH = 1280
    WINDOW_HEIGHT = 720
    REFRESH_RATE = 60

    [slang]
    {
        target = "spirv"
        profile = "glsl_460"

        [NBodyVertex]
        {
            MAX_BODIES = 512
            WORKGROUP_SIZE = 32
        }

        [NBodyFragment]
        {
            MAX_BODIES = 512
            WORKGROUP_SIZE = 32
        }
    }
}
